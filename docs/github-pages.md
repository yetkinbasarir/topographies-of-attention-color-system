# GitHub Pages

This repository includes a root `index.html` file so the latest visual reference can be published directly with GitHub Pages.

## Recommended Pages settings
- Branch: `main`
- Folder: `/ (root)`

The root `index.html` is a copy of:

`reference/t-of-a-day-night-system-v11.html`
